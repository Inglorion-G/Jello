<h1>Boards!</h1>
<ul>
	<% boards.each(function(board){ %>
	  <li>
			<a class="board-link" href="#/boards/<%= board.escape('id') %>"><%= board.escape('title') %></a>
		</li>
	<% }) %>
</ul>
<a href="#/boards/new">Add a Board!</a>